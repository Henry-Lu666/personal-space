---
title: 个人网站优化执行手册 V2.1
date: 2026-07-19
version: V2.1
parent_doc: 01-个人画像/个人网站优化建议.md（策略层）
本文档: 执行层 — 限制 + 步骤 + 验收标准 + 踩坑实录
适用范围: /mnt/d/AI/my-knowledge-base/07个人网站/ 全站所有 HTML/CSS/JS
---

# 个人网站优化执行手册 V2.1

> 本文档是策略文档 `01-个人画像/个人网站优化建议.md` 的执行落地版。
> 策略层定位、为什么这么做 → 看母文档。
> 本文档只回答：具体怎么改、限制是什么、怎么算改对了、陷阱在哪。

**本版写给执行 agent 自己看。你不需要理解为什么，只需要按步骤做。**
**每一步都有：命令、验证、失败时怎么办。照做即可。**

---

## 〇、你（执行 agent）必须遵守的 7 条铁律

### 铁律1：定位永不漂移
网站定位 = **面试后加分**（HR 收到简历 → 点开链接 → 30 秒内验证简历为真、看懂思考深度、决定约不约面）。
- ❌ 不做 SEO 主动获客的内容
- ❌ 不追求"让读者品出能力"的慢动作设计
- ✅ 30 秒内让 HR 确认简历为真、看懂思考深度、决定约不约面

### 铁律2：层级不跳步
```
P0 结构砍 → P1 内容锚点 → P2 文案润色 → P3 风格点缀
每步之间 24 小时冷却，严禁并行、严禁跳步
```
P0 没稳前不改任何文案、不动任何颜色。

### 铁律3：每次 patch 前先读现状
V1→V9 的根因是"没读 spec 就开干"。每次动手前：
1. `cd /mnt/d/AI/my-knowledge-base/07个人网站 && git status --short`  确认工作区状态
2. `git log --oneline -3`  看最近 commit
3. 不要假设行号精确——用 `grep -n "关键字" file.html` 定位真实行号
4. patch 工具的 `old_string` 要从真实文件内容复制，不要凭记忆

### 铁律4：CRLF 行尾——最危险的陷阱（已踩坑）

**所有 HTML/CSS 文件都是 CRLF 行尾（`\r\n`）。**

如果你用 `patch` 工具改 HTML 文件：
- patch 默认用 LF 替换，会破坏缩进一致性
- 改完后 `git diff` 会看到整块文件都变了（因为 `\r\n` → `\n`）

**正确做法：改 HTML 文件时用 Python 二进制操作，不用 patch 工具。**

模板：
```python
path = "/mnt/d/AI/my-knowledge-base/07个人网站/career.html"
with open(path, "rb") as f:
    data = f.read()

# 要删除的块（CRLF 行尾，从真实文件复制 byte）
target = (
    b'        <article class="feature-card rev">\r\n'
    b'          <div class="metric">XXX</div>\r\n'
    b'          ...\r\n'
    b'        </article>\r\n'
)

if target in data:
    data = data.replace(target, b'', 1)
    with open(path, "wb") as f:
        f.write(data)
    print("OK")
else:
    print("NOT FOUND — 需要查看真实文件 byte")
```

**如果非要用 patch 工具**（比如改 CSS、改 md），改后必跑：
```bash
git diff --stat
```
如果某个文件 diff 数量异常大（>50 行改动），说明 CRLF 被破坏了。回退：
```bash
git checkout <file>
```
改用 Python 二进制方式重做。

### 铁律5：不要假设 git 仓库是否存在——先验证再动手
07个人网站/ 自己就是独立 git repo（.git 在它自己根目录下，父级 my-knowledge-base 也是 repo 但是另一个）。
```bash
cd /mnt/d/AI/my-knowledge-base/07个人网站 && git rev-parse --show-toplevel
# 应返回 /mnt/d/AI/my-knowledge-base/07个人网站
```
父 repo `/mnt/d/AI/my-knowledge-base/.git` 是 KB 主 repo，07 是它的子目录但**也是独立 repo**（嵌套但互不相关——不要在父 repo 里 git add 07 目录的文件）。

### 铁律6：不编造数字，不创造简历里没有的内容
涉及"加验证锚点"或"加旁白"的任务，如果数字/案例在简历源 docx 里查不到：
- ❌ 不要加
- ❌ 不要凭印象编
- ✅ 跳过这条，在 PROGRESS.md 标注 `SKIPPED — 简历未背书`
简历源路径见末尾"资源清单"。

### 铁律7：截图验证看不了像素时的处理
当前模型（GLM-5.2 及部分模型）视觉工具返回 400 错误是**模型层限制**，不是工具坏了。处理方式：
1. 用 Playwright 截图存到 `/tmp/p{level}_{action}_after.png`
2. 用 DOM 验证替代视觉验证：
   ```python
   from playwright.sync_api import sync_playwright
   with sync_playwright() as p:
       browser = p.chromium.launch(headless=True)
       page = browser.new_page(viewport={'width':1400,'height':900})
       page.goto("http://localhost:8765/目标页.html")
       page.wait_for_load_state("networkidle")
       # 数 feature-card 数量
       count = page.locator(".feature-card").count()
       # 看 console error
       errors = []
       page.on("pageerror", lambda e: errors.append(str(e)))
       # 看 bounding box 判断布局错位
       box = page.locator(".feature-card").first.bounding_box()
   ```
3. 只要 console 0 error + DOM 元素数对 + bounding box 在合理范围 = 验证通过
4. 截图留给人类用户最后审

---

## 一、全局限制（禁区清单——不可越界）

### A. 风格禁区

| 禁项 | 理由 |
|------|------|
| ❌ 主背景切大面积纯黑 | 亮屏可读性差 + HR 截图发领导糊一片 |
| ❌ 引入仙侠/水墨/水墨人物 | 叙事元素 ≠ 信息架构元素 |
| ❌ 引入长杖、暗月、镜像对称构图 | 是插画要素，不是网站设计要素 |
| ❌ dark ocean 主风格切 dark gilded | 换皮不改良，重蹈 V1-V9 覆辙 |
| ❌ "鎏金"用超过三处 | 廉价感来自过度金属感 |

### B. 内容禁区

| 禁项 | 理由 |
|------|------|
| ❌ 同一数字在 3 个以上位置重复 | 重复稀释信息强度 |
| ❌ "独立全链路""业务原生"等空词 | 印证路径下 HR 反感自夸型形容词 |
| ❌ "3 小时训练 + 24 小时论文"放首页 | 赶工叙事降低专业感 |
| ❌ "Powered by AI" 标注 | 设计红线延续 |

### C. 结构禁区

| 禁项 | 理由 |
|------|------|
| ❌ 首页同时服务"求职"和"内容发布平台" | 信息密度冲突，目标互斥 |
| ❌ 三栏等宽卡片布局 | skill 设计红线 |
| ❌ Hero "Welcome to my portfolio" | skill 设计红线 |
| ❌ 大圆角（>12px） | skill 设计红线 |

### D. 流程禁区

| 禁项 | 理由 |
|------|------|
| ❌ 跳步执行 | 每层不稳就动下层，重复 V1-V9 失败 |
| ❌ 多文件并行改 | 错误定位困难，回退成本高 |
| ❌ 改完不验证就 commit | 用户铁律：说完"搞定"前必须实际触发通过 |
| ❌ 用 patch 改 HTML 文件而不用二进制方式 | CRLF 陷阱（见铁律4） |

---

## 二、当前真实进度状态（执行前必读）

**P0-1 已完成（2026-07-19，commit 363135d）**：
- career.html "核心卖点"卡片已删，3卡变2卡
- git log 看得到

**未做（按顺序）**：
- P0-2a：index.html 删 journey banner
- P0-2b：index.html 4入口砍2入口（需用户选方案A/B，暂跳过）
- P0-3：about.html 加 journey 链接
- P1-1到P1-4、P2-1到P2-3、P3-1到P3-2 全部未做

**下次开工第一步**：
```bash
cd /mnt/d/AI/my-knowledge-base/07个人网站 && git log --oneline -5
```
看 commit `363135d` 在不在。如果在，P0-1 已完成，从 P0-2a 开始。
如果不在，说明你处于另一个分支或状态，先和工作区主人确认。

---

## 三、P0 结构砍——详细执行单

### P0-2a：index.html 删除 journey banner

**为什么**："3 小时训练 + 24 小时论文"赶工叙事降低专业感，不该在首页首屏。

**目标文件**：`/mnt/d/AI/my-knowledge-base/07个人网站/index.html`

**当前真实状态**（执行前自己再 grep 确认）：
```bash
grep -n "journey-banner-hero" /mnt/d/AI/my-knowledge-base/07个人网站/index.html
# 预期: 1 行（大概在第59行，但不要假设）
```

**操作**：用 Python 二进制删除整个 `<a class="journey-banner journey-banner-hero rev"...>...</a>` 块。

**执行模板**：
```python
path = "/mnt/d/AI/my-knowledge-base/07个人网站/index.html"
with open(path, "rb") as f:
    data = f.read()

# 从 <a class="journey-banner journey-banner-hero 开始
# 到 </a> 结束（含尾随换行）
# 先 grep 找到起止行，然后从 file 直接复制 byte
# 不要手敲 byte——从文件 copy 出来粘贴

# 验证：
# 1. grep -n journey-banner-hero index.html 应返回 0
# 2. journey.html 本身不能删
# 3. feature-card / portal-card 数量不变
```

**改后验证清单**：
```bash
# 必须全部通过
grep -c "journey-banner-hero" index.html  # 预期 0
ls journey.html  # 文件必须存在
grep -c "portal-card" index.html  # 预期 4（portal-card 没动）
```

```python
# DOM 验证（启动 server 后跑）
count = page.locator(".journey-banner-hero").count()  # 预期 0
portal = page.locator(".portal-card").count()  # 预期 4
errors = []
page.on("pageerror", lambda e: errors.append(str(e)))
# errors 必须为空
```

**验收**：
- [ ] `grep -c "journey-banner-hero" index.html` 返回 0
- [ ] `ls journey.html` 文件仍在
- [ ] `grep -c "portal-card" index.html` 仍为 4
- [ ] Playwright DOM：`.journey-banner-hero` count = 0
- [ ] Playwright console error = 0

**回退**：`git checkout index.html`

---

### P0-2b：4 入口卡片砍成 2 入口（需要用户决策，不要擅自执行）

**这一条需要用户先选方案**：
- 方案 A（保守）：4→4，不动
- 方案 B（激进）：career+projects 合一页，research 归进去，studio+about 合一页，4→2

**你（agent）的动作**：
- 不要自己选
- 不要擅自执行
- 如果用户没回答，跳过此条，继续 P0-3

---

### P0-3：about.html 加 journey 链接

**为什么**：journey 叙事是亮点不能丢，但不该在首页首屏。

**目标文件**：`/mnt/d/AI/my-knowledge-base/07个人网站/about.html`

**操作**：在 about 页 footer 之前合适位置加一个低调链接。

**先读现状**：
```bash
grep -n "</footer>\|<footer" /mnt/d/AI/my-knowledge-base/07个人网站/about.html
```

**改动内容**（在 footer 标签开始之前加）：
```html
<p class="journey-link-subtle" style="font-size:.85rem;color:var(--muted);margin:2rem 0;">
  <a href="journey.html" style="color:var(--muted);text-decoration:underline">查看 90 天战报：毕设、建站、第一支视频 →</a>
</p>
```

**怎么操作**：用 Python 二进制方式在 footer 标签之前插入这段 HTML。先 grep 找到 footer 起始位置，读 byte，在它之前 join 新内容。

**执行模板**：
```python
path = "/mnt/d/AI/my-knowledge-base/07个人网站/about.html"
with open(path, "rb") as f:
    data = f.read()

# 找到 <footer 的位置
footer_start = data.find(b"<footer")
if footer_start == -1:
    print("NOT FOUND <footer")
else:
    insert_html = (
        b'<p class="journey-link-subtle" style="font-size:.85rem;color:var(--muted);margin:2rem 0;">\r\n'
        b'  <a href="journey.html" style="color:var(--muted);text-decoration:underline">\xe6\x9f\xa5\xe7\x9c\x8b 90 \xe5\xa4\xa9\xe6\x88\x98\xe6\x8a\xa5\xef\xbc\x9a\xe6\xaf\x95\xe8\xae\xbe\xe3\x80\x81\xe5\xbb\xba\xe7\xab\x99\xe3\x80\x81\xe7\xac\xac\xe4\xb8\x80\xe6\x94\xaf\xe8\xa7\x86\xe9\xa2\x91 \xe2\x86\x92</a>\r\n'
        b'</p>\r\n'
    )
    data = data[:footer_start] + insert_html + data[footer_start:]
    with open(path, "wb") as f:
        f.write(data)
    print("INSERTED")
```

**改后验证**：
```bash
grep -c "journey.html" /mnt/d/AI/my-knowledge-base/07个人网站/about.html  # 预期 ≥1
```

**验收**：
- [ ] `grep -c "journey.html" about.html` 返回 ≥1
- [ ] 链接可点击跳到 journey.html（Playwright click 验证）
- [ ] 视觉权重低于 H1（用 DOM 验证 font-size ≤0.9rem）

**回退**：`git checkout about.html`

---

### P0 总体验收（P0-2a + P0-3 完成后跑）

```bash
cd /mnt/d/AI/my-knowledge-base/07个人网站
echo "=== P0 总体验收 ==="
echo "1. 核心卖点删除(P0-1已做): "; grep -c "核心卖点" career.html  # 应 0
echo "2. journey banner移出首页: "; grep -c "journey-banner-hero" index.html  # 应 0
echo "3. about页journey链接: "; grep -c "journey.html" about.html  # 应 ≥1
echo "4. journey.html文件仍在: "; ls journey.html | wc -l  # 应 1
echo "5. portal-card未动: "; grep -c "portal-card" index.html  # 应 4
echo "6. git工作区状态: "; git status --short | head -5
```

全过 → 等 24 小时冷却 → 进 P1。
任一不过 → 不要 commit，按回退方案处理。

---

## 四、P1 内容锚点——详细执行单

> P1 在 P0 完成 **且 24 小时冷却后** 开始。冷却期不要并行做任何 P1 动作。

### P1-1：数字加"验证锚点"

**原则**：每个"宣称型数字"旁要能查到来源或具象证据。

**前置必读**：先从简历 docx 抽取真实数字（见末尾"资源清单"）。

**第一性原则**：简历里查不到的数字 = 不加。宁可少加不编造。

**改动清单**（每条改前先 grep 确认数字还在那位置）：

| 数字 | 现在写法 | 锚点内容 | 加在哪 |
|------|---------|---------|--------|
| 650+ 人 | "累计带过的团队规模" | "横跨 4 城市直营体系" | Hero stats 或 timeline |
| 85% 人效 | Hero stats + timeline | "3 月内达成体系双标杆" | 佛山直营店 timeline |
| 0.6705 F1 | projects ABSA 卡片 | "5 组种子消融验证" | ABSA 卡片 |
| 15,000+ 反馈 | timeline 早期 | "柳州单月单城 6000 台" | timeline 早期经历 |

**操作流程**（每个数字一条）：
1. 先打开简历 docx 验证数字真实存在
   ```python
   from hermes_tools import read_file
   r = read_file("/mnt/d/A-个人/个人简历/卢鸿璋_业务运营经理_区域总经理_数字化专家_双语简历_最终版.docx")
   print(r)
   # grep 这个数字在不在
   ```
2. 如果简历有 → 用 Python 二进制方式加锚点到对应 HTML
3. 如果简历没有 → **跳过** 这条，在 PROGRESS.md 标 `SKIPPED-简历未背书`
4. Playwright DOM 验证该字符串在页面里
5. commit

**每条独立 commit**，不要 4 条打包。
commit message 格式：`P1-1: 650人加锚点"横跨4城市直营体系"(简历背书)`

**验收**（每条）：
- [ ] 简历 docx 里有该校验数字
- [ ] HTML grep 该锚点字符串返回 ≥1
- [ ] Playwright DOM 内 text_content 包含新加锚点

---

### P1-2：about.html 加"价值边界"段

**改动**：在 about 页合适位置（建议在主介绍和联系方式之间）加：

```html
<section class="value-boundary">
  <h3>被问过最多的问题</h3>
  <p>你懂 AI，跟纯技术背景的人有什么区别？</p>
  <p class="answer">大部分 AI 项目死掉，不是因为技术做不出来，是因为做出来没人接——组织没调、利益没分、人机边界没划清楚。我 12 年不是在写代码，是在管这三件事。技术只是手段，手段不缺，缺的是有人先想清楚：谁来做、做完了谁受益、谁该干哪块。</p>
</section>
```

**操作**：用 Python 二进制方式在 about.html 现有 main content 末尾、footer 之前插入。先 grep 找定位锚点。

**验收**：
- [ ] `grep -c "被问过最多的问题" about.html` 返回 ≥1
- [ ] 内容无空词、无简历查不到的数字
- [ ] Playwright DOM 验证 .value-boundary 元素存在

---

### P1-3：Hero "求职中" pill 强化

**现状**：`index.html` 第48行附近有 `<span class="pill">求职中 · 欢迎联系</span>`（**不要假设行号**，grep 确认）。

**操作**：先 grep 找到这条 pill 的 class 和 style，看是不是已经有特殊样式。

```bash
grep -n "求职中" /mnt/d/AI/my-knowledge-base/07个人网站/index.html
```

**改动方向**：
- 提到底色对比更强（已是 `#d4a574` 边框，加深 + 放大）
- 可考虑挪到 hero-cta 行旁边

**这一条有主观审美成分**——改完截图留给用户拍板，不要自己 commit。

**验收**：
- [ ] 截图存到 `/tmp/p1_3_after.png`
- [ ] 在 commit message 注明"等用户审美确认"

---

### P1-4：footer 加 LinkedIn

**先查现状**：
```bash
grep -rn "linkedin" /mnt/d/AI/my-knowledge-base/07个人网站/ --include="*.html" | head
```

LinkedIn URL 在简历里：`www.linkedin.com/in/鸿璋-卢-04458b388`

**改动**：`index.html` 和 `about.html` footer 加 LinkedIn 文字链接（不用图标，用 `<a>` 文字）。

**操作**：Python 二进制方式，在每个文件的 `<footer>...<div class="footer-links">...` 里加一个 `<a href="...linkedin...">LinkedIn</a>`。

**验收**：
- [ ] 每个 footer page grep `linkedin.com` 返回 ≥1
- [ ] 链接 `rel="noopener"` 已加
- [ ] Playwright click 验证链接 href 正确

---

## 五、P2 文案润色——详细执行单

> P2 在 P1 完成 **且 24 小时冷却后** 开始。

### P2-1：projects.html 加"利润设计"旁白

**⚠️ 前置确认——这条最容易踩坑**：
"首单 45 万，成本价出的"这条数字**简历里有没有不确定**。

**必做前置**：
```python
from hermes_tools import read_file
r = read_file("/mnt/d/A-个人/个人简历/卢鸿璋_业务运营经理_区域总经理_数字化专家_双语简历_最终版.docx")
# 在 r['content'] 里 grep "45" "成本价" "接驳车"
```

**三种情况处理**：
- 简历有"45 万成本价" → 加旁白，照下面 HTML 改
- 简历没有 → **跳过此条**，PROGRESS.md 标 `SKIPPED-简历未背书`
- 简历有相近但不完全一致 → 用简历里的真实数字，不要用"45万"

**如果简历验证通过，改动内容**：在佛山直营店项目卡片描述末尾加：
```html
<p class="aside-note" style="font-style:italic;color:var(--muted);font-size:.9rem;margin-top:.5rem;">首单 45 万，成本价出的。先亏一个案例换市场信任。后续定价策略不是算出来的，是跟客户喝出来的——谁拿大头、分几层、什么时候提价。这比技术方案难十倍。</p>
```

**验收**：
- [ ] 简历 docx 里有该数字（已打印确认结果）
- [ ] `grep -c "aside-note" projects.html` 返回 ≥1
- [ ] Playwright DOM `.aside-note` 元素存在且 italyc样式生效

---

### P2-2：career.html "核心差异化" 句改写

**现状**（执行前再 grep 确认）：
```bash
grep -n "核心差异化是" /mnt/d/AI/my-knowledge-base/07个人网站/career.html
```

**改为**：
> 业务端到建模端我独立走过全链路：从用户反馈数据清洗到 ABSA 模型训练到消融验证到策略输出，每一步都自己动手做过。跟技术团队能聊架构细节，跟管理层能讲清业务闭环——不是"我懂两边"，是两条都走过。

**操作**：用 Python 二进制替换字符串（不是替换整块 HTML，只替换那段 `<p>` 内的中文文本，保留 `</p>` 和其它标签）。

**验收**：
- [ ] `grep -c "业务原生" career.html` 返回 0
- [ ] `grep -c "两条都走过" career.html` 返回 ≥1

---

### P2-3：AI 描述去"独立全链路"

**先 grep 现状**：
```bash
grep -rn "独立全链路\|独立 from\|独立完成从" /mnt/d/AI/my-knowledge-base/07个人网站/ --include="*.html"
```

**改为**：具体角色描述。例：
- 旧："独立完成从数据清洗到模型训练"
- 新："负责数据清洗到训练全部环节（本人主导，PM 提供需求评审）"

**注意**：每条改法可能不一样，看上下文。无团队信息则保留单兵但去掉"全链路"自夸。

**验收**：
- [ ] `grep -rn "独立全链路"` 全站 0 行
- [ ] `grep -rn "独立完成从" projects.html research.html career.html` 检查是否还有

---

## 六、P3 风格点缀——鎏金实施

> P3 在 P2 完成 **且 24 小时冷却后** 开始。

### P3-1：鎏金色值确认

**主色值**：`#d4a843`（site.css 现有 `--accent`），不加深。

### P3-2：三处实施位置

| # | 位置 | 实施 |
|---|------|------|
| 1 | section divider | `.wave` 已是渐变金线，保留不动 |
| 2 | 关键数字 metric | `.metric` 已在用 `--accent`，保留；如需强金感给 Hero `.stat strong` 改 accent |
| 3 | logo 角落 | nav-logo "HL" hover 加 `text-shadow: 0 0 1px var(--accent)` |

**实操**：

**P3-2a**：Hero stats 的 `.stat strong` 颜色改 accent
- 先 grep 现状：
  ```bash
  grep -n "\.stat strong\|stat.*strong" /mnt/d/AI/my-knowledge-base/07个人网站/css/site.css
  ```
- 如果已有 color 定义，改成 `color: var(--accent)`；如果没有，加一段

**P3-2b**：nav-logo hover 描边
- grep `.nav-logo` 在 site.css 里的 hover 状态
- 加或改 `:hover { text-shadow: 0 0 1px var(--accent); }`

**CSS 文件不是 CRLF**——可以用 patch 工具改（但改后 git diff --stat 看一眼）。

**验收**：
- [ ] `grep -c "d4a843" css/site.css` 最多 +2 使用点
- [ ] 主背景 image URL 未变（grep hero-bg url）
- [ ] Playwright 截图截图，留用户审

### P3-3 禁止动作（再次提醒）

- ❌ 不改主背景
- ❌ 不加金粉洒落 / 粒子动画
- ❌ 不加烫金立体字效
- ❌ 正文文字大面积涂金

---

## 七、单 task 完成检查表（每条改完填一次）

```
Task ID: P_-_
日期:
执行者:

[ ] git status 改动文件 ≤ 3 个
[ ] patch 完成（如改 HTML 用 Python 二进制方式，CSS 用 patch 工具）
[ ] 改后 grep 验证关键字段
[ ] Playwright DOM 截图存到 /tmp/p{level}_{action}_after.png
[ ] Playwright console errors = 0
[ ] 内容来源已确认（简历 / 用户告知 / skill 已验证）
[ ] 未编造任何数字
[ ] 未触碰禁区清单任何一项
[ ] git commit 信息格式: "P{level}-{action}: one-line summary"
[ ] 更新 PROGRESS.md 此项状态
[ ] 24 小时冷却再进下一条
```

---

## 八、回退流程（任何 task 失败）

1. **未 commit**：`git checkout <file>` 即时回退
2. **已 commit**：`git revert <sha>`（保留历史透明）
3. **PROGRESS.md 加一行**：`#### ROLLBACK：<date> <reason>`

**早回退早止损。V1→V9 的根因就是没及时回退。**

---

## 九、执行顺序总览表

| 阶段 | Task ID | 任务 | 预估时长 | 冷却 | 状态 |
|------|---------|------|---------|------|------|
| P0 | P0-1 | career.html 删核心卖点卡片 | 10 min | 24h | ✅ 已完成 |
| P0 | P0-2a | index.html 移除 journey banner | 10 min | 24h | 待做 |
| P0 | P0-2b | 4 入口砍 2 入口 | 待定 | 24h | 等用户选方案 |
| P0 | P0-3 | about.html 加 journey 链接 | 5 min | 24h | 待做 |
| P1 | P1-1 | 关键数字加验证锚点 | 60 min | 24h | 待做 |
| P1 | P1-2 | about.html 加价值边界段 | 15 min | 24h | 待做 |
| P1 | P1-3 | Hero 求职中 pill 强化 | 10 min | 24h | 待做 |
| P1 | P1-4 | footer 加 LinkedIn | 10 min | 24h | 待做 |
| P2 | P2-1 | projects 利润设计旁白（需数字验证） | 30 min | 24h | 待做 |
| P2 | P2-2 | career 核心差异化句改写 | 15 min | 24h | 待做 |
| P2 | P2-3 | AI 描述去"独立全链路" | 20 min | 24h | 待做 |
| P3 | P3-1 | 鎏金色值确认 | 10 min | 24h | 待做 |
| P3 | P3-2 | 三处鎏金实施 | 30 min | 24h | 待做 |

---

## 十、资源清单（所有外部依赖）

### Git 仓库

```
07个人网站 repo:      /mnt/d/AI/my-knowledge-base/07个人网站/.git
                     → 嵌套但独立，所有 07 改动在这里 commit
KB 主 repo:           /mnt/d/AI/my-knowledge-base/.git
                     → 父 repo，01-个人画像 策略文档在这里 commit
                     → 不要在主 repo 里 git add 07 文件
```

### 简历源（数字验证）

**主路径**：`/mnt/d/A-个人/个人简历/卢鸿璋_业务运营经理_区域总经理_数字化专家_双语简历_最终版.docx`

**注意（踩坑教训）**：
- 手册早期版本写的 `/mnt/d/个人/个人简历/` 路径**不存在**（错了）
- 实际在 `/mnt/d/A-个人/个人简历/`（注意 A-前缀）
- 该目录有十几个按公司拆分的简历版本，通用源用上面那条
- `read_file` 能自动提取 .docx 文本，不用 pandoc

### 相关文档

- 策略层 → `01-个人画像/个人网站优化建议.md`
- 设计红线 → `07个人网站/README.md`
- 进度记录 → `07个人网站/PROGRESS.md`（每完成一条加 commit + 时间戳）
- resume skill → `personal-site skill`
- 设计系统详情 → `personal-site skill > references/design-system.md`

### 本地 server（截图验证用）

```bash
cd /mnt/d/AI/my-knowledge-base/07个人网站
python3 -m http.server 8765 &
# 截图完后记得 kill
```

注意：`browser_navigate` 工具不能直接访问 localhost（被安全策略拦截），用 Playwright 脚本截图。

### Playwright 截图模板

```python
from playwright.sync_api import sync_playwright
import time

with sync_playwright() as p:
    browser = p.chromium.launch(headless=True)
    page = browser.new_page(viewport={'width': 1400, 'height': 900})
    page.goto("http://localhost:8765/目标页.html")
    page.wait_for_load_state("networkidle")
    time.sleep(1)
    page.screenshot(path="/tmp/p{level}_{action}_after.png", full_page=True)
    browser.close()
```

如果报 "Executable doesn't exist"：`playwright install chromium`

---

## 十一、踩坑实录（前车之鉴，不要再犯）

### 坑1：CRLF 行尾陷阱
- 现象：用 patch 改 HTML，git diff 看到几十行变化
- 原因：HTML 文件是 CRLF（`\r\n`），patch 默认用 LF 替换
- 解决：改 HTML 用 Python 二进制操作，改 CSS/MD 用 patch（改后 git diff --stat 检查）

### 坑2：错误路径假设
- 现象：手册写 `/mnt/d/个人/个人简历/`，实际不存在
- 原因：文档编造路径
- 解决：任何路径用前先 `ls` 验证

### 坑3：视觉工具硬限制
- 现象：`vision_analyze` 返回 "messages.content.type 参数非法"
- 原因：当前模型（GLM-5.2）不支持图像 content block
- 解决：用 Playwright DOM 验证替代（数元素 + bounding box + console error）

### 坑4：误判 git 仓库状态
- 现象：另一个 agent 评估说 "07个人网站 不是 git repo"
- 原因：没在该目录下跑 git 命令，或被父 repo 干扰
- 解决：`cd 07个人网站 && git rev-parse --show-toplevel` 确认
- 教训：blocker 判断前**先跑验证命令**，不要凭想象评估

### 坑5：PROGRESS.md 起点不一致误判
- 现象：另一个 agent 说"PROGRESS.md 与手册起点不一致，需 diff"
- 原因：过度谨慎，没 grep 实际状态
- 解决：执行前 grep 三个 P0 关键字确认是否已做
- 教训：状态判断靠 grep，不靠想象

### 坑6：git index.lock 文件冲突
- 现象：git commit 时报 "Unable to create .../index.lock: File exists"
- 原因：前一个 git 进程意外退出
- 解决：`rm -f .git/index.lock` 后重试

---

## 十二、母文档引用

- 策略层定位 → `01-个人画像/个人网站优化建议.md` § 一
- 设计红线 → `07个人网站/README.md` § 设计红线
- 进度记录 → `07个人网站/PROGRESS.md`
- 简历源 → `资源清单` 节
- 简历同步 checklist → `personal-site skill > references/resume-sync-checklist.md`

---

## 十三、版本追踪

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| V2.0 | 2026-07-19 | GLM-5.2 via Hermes | 初版执行手册 |
| V2.1 | 2026-07-19 | GLM-5.2 via Hermes | 加入踩坑实录 + CRLF 警告 + Python 模板 + 进度状态 + "写给执行 agent"语气 |
