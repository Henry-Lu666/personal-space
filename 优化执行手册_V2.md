---
title: 个人网站优化执行手册 V2
date: 2026-07-19
version: V2.0
parent_doc: 01-个人画像/个人网站优化建议.md（策略层）
本文档: 执行层 — 限制 + 步骤 + 验收标准
适用范围: /mnt/d/AI/my-knowledge-base/07个人网站/ 全站所有 HTML/CSS/JS
---

# 个人网站优化执行手册 V2

> 本文档是策略文档 `01-个人画像/个人网站优化建议.md` 的执行落地版。
> 策略层定位、为什么这么做 → 看母文档。
> 本文档只回答：具体怎么改、限制是什么、怎么算改对了。

---

## 〇、执行铁律（先读再动手）

### 铁律1：定位锚定永不漂移
网站定位 = **面试后加分 / 印证路径**（简历附链接 → HR 点开验证）
- ❌ 不做 SEO 主动获客的内容
- ❌ 不追求"让读者品出能力"的慢动作设计
- ✅ 30 秒内让 HR 确认简历为真、看懂思考深度、决定约不约面

### 铁律2：层级不跳步
```
P0 结构砍 → P1 内容锚点 → P2 文案润色 → P3 风格点缀
每步之间 24 小时冷却，严禁并行、严禁跳步
```
P0 没稳前不改任何文案、不动任何颜色。

### 铁律3：每次 patch 前先读现状
V1→V9 的根因是"没读 spec 就开干"。每次动手前：
1. `git status --short`  确认工作区干净
2. `git log --oneline -3`  确认无未提交变更
3. `patch` 工具读 old_string 时用真实文件内容，不假设行号精确（文件会变）

### 铁律4：不替代专业判断
本文档给出具体位置和措辞建议，但真实改动前要走 `patch` 工具的 fuzzy 匹配。
如果 patch 报"old_string not found"，去看真实文件内容，不要硬塞。

---

## 一、全局限制（禁区清单 — 不可越界）

### A. 风格禁区

| 禁项 | 理由 |
|------|------|
| ❌ 主背景切大面积纯黑 | 亮屏可读性差 + HR 截图发领导糊一片 |
| ❌ 引入仙侠/水墨/水墨人物 | 叙事元素 ≠ 信息架构元素 |
| ❌ 引入长杖、暗月、镜像对称构图 | 是插画要素，不是网站设计要素 |
| ❌ dark ocean 主风格切 dark gilded | 换皮不改良，重蹈 V1-V9 覆辙 |
| ❌ "鎏金"用超过三处 | 廉价感来自过度金属感 |

### B. 内容禁区

| 禁项 | 理由 |
|------|------|
| ❌ 同一数字在 3 个以上位置重复 | 重复稀释信息强度 |
| ❌ "独立全链路""业务原生"等空词 | 印证路径下 HR 反感自夸型形容词 |
| ❌ "3 小时训练 + 24 小时论文"放首页 | 赶工叙事降低专业感 |
| ❌ "Powered by AI" 标注 | resume skill 已禁，本版延续 |

### C. 结构禁区

| 禁项 | 理由 |
|------|------|
| ❌ 首页同时服务"求职"和"内容发布平台" | 信息密度冲突，目标互斥 |
| ❌ 三栏等宽卡片布局 | skill 设计红线 |
| ❌ Hero "Welcome to my portfolio" | skill 设计红线 |
| ❌ 大圆角（>12px） | skill 设计红线 |

### D. 流程禁区

| 禁项 | 理由 |
|------|------|
| ❌ 跳步执行 | 每层不稳就动下层，重复 V1-V9 失败 |
| ❌ 多文件并行改 | 错误定位困难，回退成本高 |
| ❌ 改完不截图验证 | 你 Profile 第3条铁律：说完"搞定"前必须实际触发通过 |

---

## 二、P0 结构砍 — 详细执行单

### P0-1：career.html 删除"核心卖点"卡片

**为什么**：12年/650人数字在 Hero stats + timeline 已出现两次，第三次是稀释。

**目标位置**：`/mnt/d/AI/my-knowledge-base/07个人网站/career.html`

**改动锚点**（以 patch 实际匹配为准）：
```html
<article class="feature-card rev">
  <div class="metric">定位</div>
  <h3>核心卖点</h3>
  <p>12 年新能源产业链操盘经验 + 独立建模能力（ABSA / 审计校对 / 跨境智审）。不仅仅是「懂业务、会用 AI 工具」，而是能独立把 AI 从数据清洗做到策略输出。</p>
</article>
```

**改为**：直接删除此 `<article>` 块（含尾随换行）。

**注意事项**：
- 删除后 `card-grid` 内剩 2 张卡片，CSS grid 布局应自动重排，无需改 CSS
- 删除前备份：`cp career.html career.html_v2_p0_backup.html`
- 删除后跑 `grep -n "核心卖点" career.html` 必须返回 0 行

**验收标准**：
- [ ] `grep -n "核心卖点" career.html` 返回 0 行
- [ ] `grep -n "独立建模能力（ABSA" career.html` 返回 0 行
- [ ] career 页面截图：3 卡片变 2 卡片，布局无错位
- [ ] "AI 落地 × 业务操盘" 主卡片仍在第一张

**回退方案**：`git checkout career.html` 或 `cp career.html_v2_p0_backup.html career.html`

---

### P0-2：index.html 首页结构砍

**为什么**：现首屏塞了 Hero / stats / journey banner / 4 入口卡片，HR 30 秒看不完核心。印证路径下首屏只要 3 元素：你是谁 + 你能给对方什么 + 一个 CTA。

**分两步执行**：

#### P0-2a：journey banner 从首页移除

**目标位置**：`index.html` 第 59-68 行 `<a class="journey-banner journey-banner-hero rev" href="journey.html">...</a>`

**改为**：删除整个 `<a>` 块，保留 journey.html 页面本身（叙事是亮点，不是首屏素材）。

**follow-up**：在 about.html 页脚或合适位置放一个"查看 90 天战报 →"小字链接（等 P0-3 一起做）。

**验收**：
- [ ] 首页 `grep -n "journey-banner-hero" index.html` 返回 0 行
- [ ] 首屏 Scroll 截图：journey banner 不见，hero 区块下方直接是 nav-portal
- [ ] journey.html 本身未删（`ls journey.html`）

#### P0-2b：4 入口卡片砍成 2 入口

**为什么**：career+projects 都是求职相关，research 也可并入 career 或独立保留，studio+about 合一页。

**建议合并路径**（两套可选，等用户确认再执行）：

**方案 A**（保守）：保留 career / projects / research / about（4→4，只删 studio 入口）
**方案 B**（激进）：career+projects 合一页（求职合集），research 归进去，studio+about 合一页（4→2）

**本阶段先不执行** — P0-2b 需要先和用户确认方案 A/B，本文档只标注待办。

**验收**（待执行后补）：
- [ ] 入口数量与一致方案一致
- [ ] 每个入口卡片下方 CTA 文本清晰无重复

---

### P0-3：journey 链接迁移到 about 页

**为什么**：journey 叙事是亮点不能丢，但不该在首页首屏。

**目标位置**：`/mnt/d/AI/my-knowledge-base/07个人网站/about.html`

**改动**：在 about 页 footer 之前合适位置加一个低调链接：

```html
<p class="journey-link-subtle">
  <a href="journey.html">查看 90 天战报：毕设、建站、第一支视频 →</a>
</p>
```

**CSS**：用现有 `muted` / `text-muted` 变量，字体比正文轻一级，无卡片、无按钮样式。

**验收**：
- [ ] about 页 grep "journey.html" 返回 ≥1 行
- [ ] 截图：链接低调不喧宾夺主
- [ ] 链接可点击跳到 journey.html

---

### P0 阶段总体验收（P0 全部完成后）

跑以下检查脚本，全过才进 P1：

```bash
cd /mnt/d/AI/my-knowledge-base/07个人网站
echo "=== P0 总体验收 ==="
echo "1. 核心卖点删除:"; grep -l "核心卖点" career.html 2>/dev/null || echo "  PASS"
echo "2. journey banner 移出首页:"; grep -c "journey-banner-hero" index.html
echo "3. 入口卡片数量:"
grep -c "portal-card rev" index.html
echo "4. about 页 journey 链接:"; grep -c "journey.html" about.html
echo "5. 24 小时冷却: 见 PROGRESS.md 时间戳"
echo "6. 截图对比: /tmp/p0_after_*.png vs 旧版"
```

---

## 三、P1 内容锚点 — 详细执行单

> P1 在 P0 完成且 24 小时冷却后开始。

### P1-1：数字加"验证锚点"

**原则**：每个"宣称型数字"旁要能查到来源或具象证据。

**改动清单**：

| 数字 | 现状 | 验证锚点（建议加在哪） |
|------|------|------|
| 650+ 人 | timeline "运营及人才发展官" | 加"横跨 4 城市直营体系"（核实简历是否真的 4 城） |
| 85% 人效提升 | Hero stats + timeline | 佛山卡片加"3 月内达成体系双标杆"（已在 resume skill 列出） |
| 12 年 | Hero stats | 保留（已是常识型） |
| 0.6705 F1 | projects ABSA 卡片 | 加"5 组种子消融验证"（resume 已记录） |
| 15,000+ 反馈 | timeline 早期经历 | 加"柳州单月单城 6000 台"倍数证据 |

**操作流程**：
1. 先从简历源文件抽取真实数字：`/mnt/d/个人/个人简历/卢鸿璋的简历_2026.docx`
2. 对每个要加的锚点，先确认简历里有 → 加 → 截图验证
3. 没有简历背书的数字，**保留不改、不编**（career skill 铁律）

**验收标准**：
- [ ] 每个关键数字旁能找到"具象证据"（地名/案例名/方法名）
- [ ] 没有任何简历里查不到的编造数字
- [ ] ABSA 项目卡片附"5 组种子"字样（如简历确有此描述）

### P1-2：about.html 加"价值边界"段

**改动**：about 页合适位置（建议在主介绍和联系方式之间）加：

```html
<section class="value-boundary">
  <h3>被问过最多的问题</h3>
  <p>你懂 AI，跟纯技术背景的人有什么区别？</p>
  <p class="answer">大部分 AI 项目死掉，不是因为技术做不出来，是因为做出来没人接——组织没调、利益没分、人机边界没划清楚。我 12 年不是在写代码，是在管这三件事。技术只是手段，手段不缺，缺的是有人先想清楚：谁来做、做完了谁受益、谁该干哪块。</p>
</section>
```

**验收**：
- [ ] about 页 grep "价值边界" 或 "被问过最多的问题" 返回 ≥1 行
- [ ] 截图：段落视觉权重低于 H1，不喧宾夺主
- [ ] 内容无空词、无简历查不到的数字

### P1-3：Hero "求职中" pill 强化

**现状**：`index.html` 第48行已有 `<span class="pill">求职中 · 欢迎联系</span>`，但与"12年新能源管理""Applied AI"等 pill 同等视觉权重。

**改动**：
- 提到底色对比更强（已是 `#d4a574` 边框，可加深 + 放大）
- 可考虑把它从"identity-row"挪到 hero-cta 行旁边，HR 视线先扫到 CTA

**验收**：
- [ ] 截图："求职中" pill 的视觉权重 ≥ "下载简历" CTA
- [ ] 移动端宽度下不挤下下一行

### P1-4：LinkedIn 链接补 footer

**现状**：简历上有 `www.linkedin.com/in/鸿璋-卢-04458b388`，站内不显眼。

**改动**：`index.html` 和 `about.html` footer 加 LinkedIn 文字链接（不用图标，遵循 skill 设计红线）。

**验收**：
- [ ] footer grep "linkedin.com" 返回 ≥1 行（每个有 footer 的页）
- [ ] 链接 rel="noopener" 已加

---

## 四、P2 文案润色 — 详细执行单

> P2 在 P1 完成且 24 小时冷却后开始。

### P2-1：projects.html 加"利润设计"旁白

**位置**：佛山直营店项目卡片描述末尾。

**改动**：加一行斜体 / 弱视觉色旁白（不用粗体）：
```html
<p class="aside-note">首单 45 万，成本价出的。先亏一个案例换市场信任。后续定价策略不是算出来的，是跟客户喝出来的——谁拿大头、分几层、什么时候提价。这比技术方案难十倍。</p>
```

**前置确认**：
- ⚠️ "首单 45 万，成本价出的"这条数字简历里**是否真实存在**？skill 资料中我没找到此条。执行前必须向用户确认或从简历源 docx 重新验证。
- 如果简历里没有 → 不要加 → 不编。

**验收**：
- [ ] 数字来源已验证（用户确认 / 简历抽取）
- [ ] 旁白视觉权重低于主项目描述（不喧宾夺主）
- [ ] CSS class `aside-note` 已在 site.css 新增（字体 italic + color: var(--muted)）

### P2-2：career.html "核心差异化"句改写

**现状**：
> 核心差异化是「业务原生 + 独立建模能力」，能跟技术团队聊架构细节，也能跟管理层讲业务闭环。

**改为**：
> 业务端到建模端我独立走过全链路：从用户反馈数据清洗到 ABSA 模型训练到消融验证到策略输出，每一步都自己动手做过。跟技术团队能聊架构细节，跟管理层能讲清业务闭环——不是"我懂两边"，是两条都走过。

**验收**：
- [ ] 解释从"我会什么"升级到"我走过什么"
- [ ] 删掉"独立建模能力"自夸型形容词
- [ ] grep "业务原生" career.html 返回 0 行

### P2-3：AI 项目措辞去"独立全链路"

**原则**：AI 时代"独立"暗示没团队协作经验。

**位置**：projects.html / research.html 中所有"独立全链路""独立 from X to Y"描述。

**改为**：具体角色描述，例：
- 旧："独立完成从数据清洗到模型训练"
- 新："负责数据清洗到训练全部环节（本人主导，PM 提供需求评审）"

**验收**：
- [ ] grep "独立全链路" projects.html research.html career.html 返回 0 行
- [ ] 每条 AI 描述补"团队/协作角色"信息（如有则加，无则保留单兵但禁"全链路"自夸）

---

## 五、P3 风格点缀 — 鎏金实施

> P3 在 P2 完成且 24 小时冷却后开始。

### P3-1：确定鎏金色值

**主色值**：`#d4a843`（site.css 现有 `--accent`），加深候选：`#c9962f`。

**验证色值对比可读性**：
- 在 `ocean_deep.jpg` 暗背景上：`#d4a843` 对比度约 6.8:1（达 WCAG AA），可保留为主
- `#c9962f` 对比度更高（约 7.6:1），更"熔金感"

**建议**：保留 `#d4a843`，不用加深。理由：现有 accent 已通过多轮验证，无理由增改。

### P3-2：三处实施位置

| # | 位置 | 实施 |
|---|------|------|
| 1 | section divider | `.wave` 已是渐变金线，保留不动 |
| 2 | 关键数字 metric | `.metric` 文字色已在用 `--accent`，保留；如需强金感，单独给 Hero `.stat strong` 改 accent 色 |
| 3 | logo 角落 | nav-logo "HL" 加 hover 状态下鎏金描边（`text-shadow: 0 0 1px var(--accent)`），默认态不变 |

**措辞细节**：
- `.metric` 文字色已是 `var(--accent)` — 无需改
- Hero `.stat strong` 改 accent：先确认会否破坏 hero-stats 视觉层级
- "HL" hover 金线：低风险装饰，最后一项做

### P3-3：禁止动作（再次提醒）

- ❌ 不改主背景
- ❌ 不加金粉洒落/粒子动画
- ❌ 不加烫金立体字效
- ❌ 不在正文文字大面积涂金（只限 metric / accent）

**验收**：
- [ ] `grep -c "d4a843" css/site.css` 不显著增加（最多 +2 使用点）
- [ ] 主背景 Image URL 未变
- [ ] 截图：鎏金点缀感存在但不喧宾夺主
- [ ] screenshot /tmp/p3_after_home.png 自己看一遍确认无廉价感

---

## 六、单 task 完成检查表（每条改完填一次）

```
Task ID: P_-_
日期:
执行者:

[ ] git status 改动文件 ≤ 3 个
[ ] patch 工具 fuzzy 匹配成功
[ ] 改后 grep 验证关键字段
[ ] Playwright 截图存到 /tmp/p{level}_{action}_after.png
[ ] 内容来源已确认（简历 / 用户告知 / skill 已验证）
[ ] 未编造任何数字
[ ] 未触碰禁区清单任何一项
[ ] git commit 信息格式: "P{level}-{action}: one-line summary"
[ ] 更新 PROGRESS.md 此项状态
```

---

## 七、回退流程

任何 task 执行后发现破坏：

1. 未 commit：`git checkout <file>` 即时回退
2. 已 commit：`git revert <sha>`（保留历史透明）
3. PROGRESS.md 加一行 `#### ROLLBACK：<date> <reason>`

**回退不丢面子**。V1→V9 的教训就是没及时回退。早回退早止损。

---

## 八、执行顺序总览表

| 阶段 | Task ID | 任务 | 预估时长 | 冷却 |
|------|---------|------|---------|------|
| P0 | P0-1 | career.html 删核心卖点卡片 | 10 min | 24h |
| P0 | P0-2a | index.html 移除 journey banner | 10 min | 24h |
| P0 | P0-2b | index.html 4 入口砍 → 2 入口（待方案 A/B） | 待定 | 24h |
| P0 | P0-3 | about.html 加 journey 链接 | 5 min | 24h |
| P1 | P1-1 | 关键数字加验证锚点 | 60 min | 24h |
| P1 | P1-2 | about.html 加价值边界段 | 15 min | 24h |
| P1 | P1-3 | Hero 求职中 pill 强化 | 10 min | 24h |
| P1 | P1-4 | footer 加 LinkedIn | 10 min | 24h |
| P2 | P2-1 | projects 利润设计旁白（需验证数字） | 30 min | 24h |
| P2 | P2-2 | career 核心差异化句改写 | 15 min | 24h |
| P2 | P2-3 | AI 描述去"独立全链路" | 20 min | 24h |
| P3 | P3-1 | 鎏金色值确认 | 10 min | 24h |
| P3 | P3-2 | 三处鎏金实施 | 30 min | 24h |

**总预估**：纯执行时间约 3.5 小时。含冷却约 12 天走完。

---

## 九、母文档引用

- 策略层定位 → `01-个人画像/个人网站优化建议.md` § 一、定位前置判断
- 设计红线 → `07个人网站/README.md` § 设计红线
- 进度记录 → `07个人网站/PROGRESS.md`（每完成一条在这里加 commit + 时间戳）
- 简历源（数字验证）→ `/mnt/d/A-个人/个人简历/卢鸿璋_业务运营经理_区域总经理_数字化专家_双语简历_最终版.docx`（通用源，按公司拆分的版本在同名目录）
- 简历同步 checklist → `personal-site skill > references/resume-sync-checklist.md`

---

## 十、版本追踪

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| V2.0 | 2026-07-19 | GLM-5.2 via Hermes | 初版执行手册，覆盖 P0-P3 全阶段 |

> 每完成一个 Task，在本表下补一行 commit log。
